# DECISION_LOG.md
**Owner**: @data-architect

Granular decisions beyond ADR-level.

| Date | Decision | Alternatives | Chosen | Why | Trade-off | Reversible? |
|------|----------|--------------|--------|-----|-----------|-------------|
| 2026-06-18 | How to handle Snowflake `prod` cred check failing because `NOVARTIS_STTM_ROLE`/`_WH`/`_DB` didn't exist | (a) stay on `dev`/DuckDB and defer cloud entirely to a later bounded artifact window; (b) provision the missing objects immediately so step 0 of the resume plan could pass | (b) — provisioned immediately, live, with user approval in the moment | Unblocked `dbt debug --target prod` in the same step it was being verified | @scope-guardian later vetoed this in the Phase 4 retroactive review: provisioning live cloud infra reactively, outside the brief's "short, deliberate, FinOps-gated artifact window," with zero entry in this log at decision time — exactly the undocumented-decision pattern this log exists to prevent. Retroactively logged here per that veto's required action. | Y — objects can be dropped; no data was loaded into them during this session |
| 2026-06-18 | First Snowflake grant script used `GRANT ALL ON DATABASE`/`SCHEMA PUBLIC`/`FUTURE SCHEMAS`/`FUTURE TABLES` | (a) keep `ALL` (simplest, "it's a trial account"); (b) scope to `USAGE`+`CREATE SCHEMA` at db level, let the role own what it creates | (b) after @data-architect vetoed (a) in `DEBATE_LOG_phase_4.md` | `ALL` grants ownership-class privileges (DROP/MANAGE GRANTS) for a role that only needs to create+write its own schemas — least-privilege violation regardless of account tier | None — functionally equivalent access for dbt's actual needs (verified: `dbt debug --target prod` still passes after the re-grant) | Y — re-ran corrected grants same session, see ADR-004 |
| 2026-06-18 | Beta ingestion: 1,000-row openFDA API sample vs. full 136,038-row bulk zip | (a) keep the 1,000-row stub as originally scaffolded; (b) switch to the documented bulk-download alternative | (b) | More product coverage directly raises crosswalk match odds against Gamma's free-text drug names (validated empirically before committing: estimated ~51% exact-name overlap on the full set vs. a much smaller, untested overlap on a 1k-row arbitrary slice) | 136x more bronze rows to process; @scope-guardian reviewed this as in-scope (fixing an unfinished stub to match the brief's own Beta definition), flagged the volume jump as a capacity-planning note for future phases, not a blocker | Y — ingestion script, re-running with the old 1k-row logic would require reverting the script |
| 2026-06-18 | `dim_drug`/`dim_condition` surrogate keys: BIGINT sequence (original stub type) vs. `dbt_utils.generate_surrogate_key` hash (VARCHAR) | (a) BIGINT via `row_number()`/sequence; (b) deterministic hash key | (b) | `dim_drug` unions two independently-generated row sources (NDC snapshot rows + ATC category seed rows) — a sequence would require coordinating numbering across both branches; a hash key is collision-free by construction and idempotent across reruns | Wider key (32-char varchar vs 8-byte bigint) — negligible at this row volume (~134k); @data-architect approved, asked it be documented as a deviation from the Phase 3 physical-model stub | Y — would require a backfill/remap if changed later, but no downstream consumers exist yet |
| 2026-06-18 | How to interpret the Phase 4→5 hard blocker "All unit tests pass" for a dbt-SQL-centric build with no standalone Python transform functions | (a) require hand-written pytest unit tests for transform logic before calling this blocker satisfied; (b) accept dbt's own test framework (50 schema/singular tests) + new integration tests as the unit-test-equivalent layer | (b), per @qa-engineer's independent review | There is no isolated Python transform logic to unit-test — all transforms are dbt SQL models, where dbt's generic+singular tests are the standard, idiomatic verification layer; insisting on (a) would mean writing tests for code that doesn't exist in this architecture | This is a *reinterpretation* of what the original blocker phrase meant when CLAUDE.md was written, not a literal satisfaction of it in the traditional sense — documented so a future reviewer doesn't assume "unit tests" means pytest-on-functions here | N/A — interpretation, not code; reversible by writing pytest unit tests later if the architecture ever grows standalone Python transform logic |
| 2026-06-18 | Sequencing Phase 5 doc-closeout against ADR-005 (S3-canonical storage pivot, landed mid-session, ~2-day migration, DPE sign-off then-pending) | (a) close out Phase 5 on the current (pre-ADR-005) build first, log ADR-005 as a separate tracked next phase, get DPE's pending sign-off now but don't start the migration; (b) pause Phase 5, start the ADR-005 migration immediately; (c) interleave — migrate incrementally while finishing Phase 5 docs | (a) — explicit user choice via AskUserQuestion | Avoids writing Phase 5 artifacts (DAG wiring, integration tests, OPS_RUNBOOK) twice — once for the current architecture, once again after a non-trivial ~2-day source-binding refactor (ADR-005 Condition D) that hadn't even gotten its platform-engineering sign-off yet | This session's DAG wiring, integration tests, and OPS_RUNBOOK.md all describe the pre-ADR-005 local-DuckDB-file architecture and will need rework once the S3 migration actually executes — explicitly accepted, not an oversight (see ADR-005's own text in CLAUDE.md/README.md flagging this) | Y — purely a sequencing choice; no migration code was written either way |
