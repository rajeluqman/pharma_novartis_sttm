# Screenshots

Evidence images referenced from the root `README.md` ("Business Questions Answered"
and "Build Evidence" sections). Drop real PNGs here using the same filename as the
`PLACEHOLDER_*` reference it replaces, e.g. `PLACEHOLDER_alpha_landing.png` →
`alpha_landing.png` (update the README link to match).
